<h4>Category*<br/><em>What is your purpose?</em></h4>
<div>
  <% @categories.each do |category| %>
    <div class="category">
      <p class="title">
        <%= f.radio_button 'category_id', category.id %>
        <%= category.title %>
      </p>
      <p class="description">
        <%= category.description%>
      </p>
    </div>
  <% end %>
</div>