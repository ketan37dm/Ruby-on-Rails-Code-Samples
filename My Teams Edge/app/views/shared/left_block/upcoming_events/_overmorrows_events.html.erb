<hr>
<span class="title"><%= Date.tomorrow + 1 %></span>
<% overmorrows_events.each do |event| %>
  <%= render partial: 'shared/left_block/upcoming_events/event', locals: {:event => event}  %>
<% end %>
